# 0512 修订说明：恢复原首页结构 + 保留性能优化

## 这次修正的方向

上一版调整过根目录首页结构，导致视觉变粗糙、入口结构不清晰。本版已恢复原有首页结构：

- 保留 `/homepage/` 源码目录。
- 保留 `/homepage/dist/index.html` 作为根目录 `/index.html` 的 iframe 首页。
- 性能优化只放在加载策略和资源体积上，不破坏原首页视觉。

## 已完成的性能优化

1. 首页阶段不再提前加载 Cesium、`app-3d.js`、无人机脚本和第一人称脚本。
2. OpenLayers 改为进入 2D 视图时再加载。
3. 首页阶段不再提前 `ensurePlanMap()` 初始化 2D 地图。
4. 2D 图层静态资源不再使用 `cache: no-store`。
5. 多个 2D 图层改为并行加载。
6. 2D overlay 渲染改成双缓冲：先构建新 `VectorSource`，再一次性替换，减少图层闪烁和“清空后等待”。
7. 3D 首屏先用椭球地形快速显示，在线地形后台加载成功后再替换。
8. 删除 3D 进入后的重复飞行动作。
9. 在线资源超时时间从 20 秒降为 6 秒。
10. 正射影像由 10MB PNG 换成约 1.4MB WebP。
11. 等高线和高程分带 GeoJSON 已简化压缩。
12. 删除未使用的 29MB 无人机源模型 `animated-drone-source.glb`。

## 当前入口关系

- `/index.html`：主平台入口，继续展示原来的 React/Vite 首页 iframe。
- `/homepage/`：原首页源码和构建产物，完整保留。
